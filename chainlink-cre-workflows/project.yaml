# Public RPCs: Alchemy URLs often 403 from CLI (origin not on allowlist). Override in .env if needed.
staging-settings:
  rpcs:
    - chain-name: ethereum-mainnet
      url: "https://ethereum.publicnode.com"

production-settings:
  rpcs:
    - chain-name: ethereum-mainnet
      url: "https://ethereum.publicnode.com"

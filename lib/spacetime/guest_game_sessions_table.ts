// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
import {
  AlgebraicType as __AlgebraicTypeValue,
  BinaryReader as __BinaryReader,
  BinaryWriter as __BinaryWriter,
  ClientCache as __ClientCache,
  ConnectionId as __ConnectionId,
  DbConnectionBuilder as __DbConnectionBuilder,
  DbConnectionImpl as __DbConnectionImpl,
  Identity as __Identity,
  SubscriptionBuilderImpl as __SubscriptionBuilderImpl,
  TableCache as __TableCache,
  TimeDuration as __TimeDuration,
  Timestamp as __Timestamp,
  deepEqual as __deepEqual,
  type AlgebraicType as __AlgebraicTypeType,
  type AlgebraicTypeVariants as __AlgebraicTypeVariants,
  type CallReducerFlags as __CallReducerFlags,
  type ErrorContextInterface as __ErrorContextInterface,
  type Event as __Event,
  type EventContextInterface as __EventContextInterface,
  type ReducerEventContextInterface as __ReducerEventContextInterface,
  type SubscriptionEventContextInterface as __SubscriptionEventContextInterface,
} from "spacetimedb";
import { GuestGameSession } from "./guest_game_session_type";
import { PlayerType } from "./player_type_type";
// Mark import as potentially unused
declare type __keep_PlayerType = PlayerType;

import { type EventContext, type Reducer, RemoteReducers, RemoteTables } from ".";
declare type __keep = [EventContext, Reducer, RemoteReducers, RemoteTables];

/**
 * Table handle for the table `guest_game_sessions`.
 *
 * Obtain a handle from the [`guestGameSessions`] property on [`RemoteTables`],
 * like `ctx.db.guestGameSessions`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.guestGameSessions.on_insert(...)`.
 */
export class GuestGameSessionsTableHandle {
  tableCache: __TableCache<GuestGameSession>;

  constructor(tableCache: __TableCache<GuestGameSession>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<GuestGameSession> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `id` unique index on the table `guest_game_sessions`,
   * which allows point queries on the field of the same name
   * via the [`GuestGameSessionsIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.guestGameSessions.id().find(...)`.
   *
   * Get a handle on the `id` unique index on the table `guest_game_sessions`.
   */
  id = {
    // Find the subscribed row whose `id` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: bigint): GuestGameSession | undefined => {
      for (let row of this.tableCache.iter()) {
        if (__deepEqual(row.id, col_val)) {
          return row;
        }
      }
    },
  };
  /**
   * Access to the `sessionId` unique index on the table `guest_game_sessions`,
   * which allows point queries on the field of the same name
   * via the [`GuestGameSessionsSessionIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.guestGameSessions.sessionId().find(...)`.
   *
   * Get a handle on the `sessionId` unique index on the table `guest_game_sessions`.
   */
  sessionId = {
    // Find the subscribed row whose `sessionId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: string): GuestGameSession | undefined => {
      for (let row of this.tableCache.iter()) {
        if (__deepEqual(row.sessionId, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: GuestGameSession) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: GuestGameSession) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: GuestGameSession) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: GuestGameSession) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: GuestGameSession, newRow: GuestGameSession) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: GuestGameSession, newRow: GuestGameSession) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
